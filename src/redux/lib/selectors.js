//Following is the format for selectors
// export const gameSelector = state => state.games;
export const savedSitesSelector = (state) => state.savedSites;
export const userCategoriesSelector = (state) => state.userCategories;
